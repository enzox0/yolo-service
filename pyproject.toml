[project]
name = "yolo-service"
version = "0.1.0"
description = "YOLO11 Inference Microservice"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "ultralytics",
    "torch",
    "pydantic>=2",
    "pydantic-settings",
    "python-multipart",
    "requests",
    "python-dotenv",
]

[tool.uv]
package = false
